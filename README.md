# met-data

```
git clone git@github.com:karcio/met-data.git
cd met-data
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
mv config.template.ini config.ini # edit config
python src/alerts.py
deactivate
```
